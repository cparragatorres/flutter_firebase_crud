import 'package:flutter/material.dart';

class DestacadoView extends StatelessWidget {
  const DestacadoView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Destacados"));
  }
}
