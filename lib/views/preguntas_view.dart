import 'package:flutter/material.dart';

class PreguntasView extends StatelessWidget {
  const PreguntasView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
