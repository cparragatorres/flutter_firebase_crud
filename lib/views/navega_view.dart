import 'package:flutter/material.dart';

class NavegaView extends StatelessWidget {
  const NavegaView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

